parameters:
  cert_manager:
    namespace: syn-cert-manager
    dns01-recursive-nameservers: "1.1.1.1:53"
    charts:
      cert-manager: v1.5.4
    http_proxy: ""
    https_proxy: ""
    no_proxy: ""
    solvers:
      nginx_http01:
        http01:
          ingress:
            class: 'nginx'
    secrets: {}
    acme_dns_api: {}
    # acme_dns_api:
    #   endpoint: acme-dns-api.example.com
    #   username: dns_api_registration_user
    #   password: dns_api_registration_password
    #   fqdns: [ "api.cluster.example.com", "apps.cluster.example.com" ]
    images:
      kubectl:
        registry: quay.io
        image: bitnami/kubectl
        tag: '1.23.3@sha256:5832d8190794d5a7fefb709f1825929d194245bbce3546988087d94f80e90b3f'

    helm_values:
      global:
        leaderElection:
          namespace: ${cert_manager:namespace}
      extraEnv:
        - name: HTTP_PROXY
          value: ${cert_manager:http_proxy}
        - name: HTTPS_PROXY
          value: ${cert_manager:https_proxy}
        - name: NO_PROXY
          value: ${cert_manager:no_proxy}
      installCRDs: true
      prometheus:
        servicemonitor:
          enabled: true
      extraArgs:
        - --dns01-recursive-nameservers="${cert_manager:dns01-recursive-nameservers}"
      resources:
        requests:
          cpu: 50m
          memory: 512Mi
      webhook:
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
      cainjector:
        resources:
          requests:
            cpu: 50m
            memory: 512Mi
